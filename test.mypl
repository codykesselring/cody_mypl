class Test{
    string name; 
    int id;
    
    void farkle(){
        print("HUMANAHUMANA!\n");
    }
    void add_id(int x, int y){
        this.id = this.id + x + y;
    }
}

struct Animal{
    string type;
    int age;
}

void main(){
    Test obj = new Test("John", 123);
    Animal cow = new Animal("cow", 12);

    print(obj.name);
    print("\n");
    print(obj.id);
    print("\n");
    obj.farkle();
    obj.add_id(2, 1);
    print(obj.id);
}
